#include <stdio.h>

int main() {
    int num = 5;
    
    printf("Output of num++: %d\n", num++);
    printf("Value of num now: %d\n", num);

    printf("Output of --num: %d\n", --num);
    printf("Value of num now: %d\n", num);
}
